using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Shop : MonoBehaviour {

    /*[SerializeField]private Transform _shop;
    private GameObject _shopPanel;
    private bool _shopIsOpened;
    private float _maxDistanceToShop = 2;

	void Start () {
		
	}
	
	void Update () {
        //CheckForShop();	
	}

    void CheckForShop()
    {
        
        float distance = Vector2.Distance(_shop.position , this.transform.position);
        if (distance <= _maxDistanceToShop)
        {
            //ToggleShop();
        }
        else if (distance > _maxDistanceToShop)
        {
            //_shopPanel.SetActive(false);
            //_shopIsOpened = false;
        }
    }

    public void ToggleShop()
    {
        if (!_shopIsOpened)
        {
            _shopPanel.SetActive(true);
            _shopIsOpened = true;
        }
        if (_shopIsOpened)
        {
            _shopPanel.SetActive(false);
            _shopIsOpened = false;
        }
    }*/
}
